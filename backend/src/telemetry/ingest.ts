/**
 * Telemetry ingestion logic lives in `vehicles/routes.ts` (POST /vehicles/:vehicleId/location).
 * This module documents the contract for high-write telemetry subcollection + hot vehicle updates.
 */
export const TELEMETRY_SUBCOLLECTION = "telemetry";
